import React from 'react';
import ProfileCard from '../../components/cards/ProfileCard'

const Profile = () => {
    return (
        <div>
            <ProfileCard />
        </div>
    );
}

export default Profile;
